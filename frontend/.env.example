VITE_FUTURE_BUILDABILITY_ENABLED=true
# Optional in local/dev. Leave empty in same-origin production behind LB + IAP.
# Example direct API URL:
# VITE_API_BASE_URL=https://terrazoning-api-xxxxx-ew.a.run.app
VITE_API_BASE_URL=
# Optional: override the default OpenFreeMap Liberty basemap with your own
# MapLibre-compatible style URL (MapTiler, Stadia, self-hosted, etc.).
# VITE_MAP_STYLE_URL=https://your-style-endpoint.example/style.json
